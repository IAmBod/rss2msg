/rss2msg
/rss2msg.db
/rss2msg.db-*
/bin/
*.test
*.out
coverage.txt
.envrc
.env
.idea/
.vscode/
.task/

# Obsidian per-user UI state (churny, machine-specific)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Claude+Obsidian tooling state — generated per-machine (absolute paths), not source
.vault-meta/

# Local working config (copy from a config.example*.yaml)
/config.yaml

# Obsidian vault config + plugins (repo doubles as a vault; local-only)
.obsidian/

# Claude Code tooling state (settings, worktrees) — local-only
.claude/
